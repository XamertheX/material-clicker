//
// Entrypoint to a seperate electron program, which is only going to run the auto updater
// which will replace itself with the game itself. This makes it so I only have to release
// the game package once (very large), and then push a small ASAR for every new update.
//

// TODO:
